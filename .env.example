# Railway provides this automatically when you attach a PostgreSQL plugin
# to this service (Add-on -> Database -> PostgreSQL). Copy the reference
# variable ${{Postgres.DATABASE_URL}} into this service's variables tab.
DATABASE_URL="postgresql://user:password@localhost:5432/ai_agent_governance"
